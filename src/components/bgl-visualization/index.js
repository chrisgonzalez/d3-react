import React, { Component, PropTypes } from 'react';
import d3 from 'd3';

require('./bgl-viz.scss');

class BGLVisualization extends Component {
    render() {
        return (
            <h2>2. Let the visualization begin!</h2>
        )
    }
}

export default BGLVisualization;
