import React, { Component, PropTypes } from 'react';
import d3 from 'd3';
import fitProjection from 'components/map/fit-map-projection'

require('./map.scss');

class Map extends Component {
    render() {
        return (
            <h2>3. Let's make a map</h2>
        );
    }
}

export default Map;
